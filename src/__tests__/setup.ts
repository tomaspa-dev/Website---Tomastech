// Global test setup — runs before every test file
// Polyfill / configure any globals needed here

// No-op: happy-dom provides localStorage, sessionStorage, and crypto.subtle natively
// Just export to mark this as a module
export {};
